<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2>Your data is:</h2>
    <table border="2px">
        <tr>
            <th>ID</th>
            <th>Username</th>
            <th>Email</th>
            <th></th>
        </tr>
         <% for(person of data) { %>
        <tr>
            <td><%= person.id%></td>
            <td><%= person.username %></td>
            <td><%= person.email %></td>
            <td><form method="get" action="/user/<%=person.id%>"><button>Edit</button></form></td>
        </tr>
        <% } %>
    </table>
   
</body>
</html>